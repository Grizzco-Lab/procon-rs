# TODO List for ProCon HID Proxy

## Console Dumper Improvements
- [x] 1. ConsoleDump should process Frame instead of raw data (&[u8])
- [x] 2. decode_procon_packet should output compact one-line button/stick status
- [x] BONUS: Refactor ProConParser to static methods (no self needed)
- [x] BONUS: ConsoleDumper now uses ProConParser::parse_input_report instead of duplicating logic
- [x] 3. Move dump operations to separate thread (the thread doesn't need high priority)
- [x] 4. Set high priority and IO priority only for main proxy thread

## Future Enhancements
- [x] Refactor the code. Looks like we only have one entry point for the moment. Make it main.go
- [x] Add command line arguments for options
- [x] Options from yaml/toml file.
- [ ] Add packet filtering capabilities (based on time range)
- [ ] Implement file-based dumper seek functionality
- [x] Implement a 3d visualization of the controller's movement
  - what is the best way to do this?



Bigger items
- [x] video capture at the same time. Save procontroller keystrokes and video to dataset (have timestamp synced).
  - video capture will be done on another more powerful device (Steam Deck).
  - make sure to run ntp to sync the device timestamp each time before starting the capture.
  - implementation approach:
    * write a script that ssh to Steam Deck and starts video capture program
    * use V4L2 (v4l crate) to capture from Elgato 4K X device
    * store video files to rclone mounted Dropbox for easy access
    * sync timestamps between devices using NTP
    * file structure: session_*/metadata.json, controller.procon, video.mp4, sync.json
  - since we have multiple devices, maybe create a k8s cluster to manage all my personal devices and package these different programs as containers. Deploy them to different nodes at once with kubectl? Lifetime management is easier.
  - where to store the dataset? → Dropbox via rclone mount
- [ ] gamestate capture
  - Is it encrypted?
  - Is it possible to sound a beep or display some effect like a desktop pet when a particular boss is detected in the game state?
  - How is desktop pet implemented? (it only covers a small area of the screen)
